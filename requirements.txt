requests>=2.31.0
# PySide6 currently ships wheels only for Python < 3.14.
PySide6>=6.7.0
matplotlib>=3.8.0
py2app>=0.28.0

# Optional Lambda dependencies (install separately if deploying to Lambda)
# boto3>=1.34.0  # Uncomment for Lambda deployment
# pyarrow>=14.0.0  # Uncomment for Parquet support
# pandas>=2.1.0  # Uncomment for data processing
# aws-lambda-powertools[tracer]>=2.30.0  # Uncomment for Lambda observability
